import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/**
 * Render =  colocando em tela
 */
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

